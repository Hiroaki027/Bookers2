<h2>Users</h2>
<table>
  <tr>
    <th>Image</th>
    <th>Name</th>
  </tr>
  <tr>
    <% @users.each do |user| %>
    <td><%= image_tag user.get_profile_image %></td>
    <td><%= user.name %></td>
    <td><%= link_to "Show",user_path(user) %></td>
    <% end %>
  </tr>
</table>