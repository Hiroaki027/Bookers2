<div>
  <%= flash[:notice] %>
</div>

<div class= "container">
  <div class= "row">
    <%= render 'books/info', user: @user, book: @book_new %>
    <div class= "col-7 offset-1">
      <h2>Books</h2>
      <table class= "table">
        <tr>
          <th>User</th>
          <th>Title</th>
          <th>Opinion</th>
        </tr>
        <% @book.each do |f| %>
         <tr>
            <td><%= image_tag f.user.get_profile_image %></td>
            <td><%= link_to f.title, book_path(f) %></td>
            <td><%= f.body %></td>
         </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
